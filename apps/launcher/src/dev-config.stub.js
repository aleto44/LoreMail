/**
 * Production stub — all fields empty so the launcher starts from scratch.
 * The real dev-config.js (with pre-filled tokens) is gitignored and only
 * used during local development.
 */
export default {};
